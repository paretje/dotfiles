#
# weechat -- trigger.conf
#

[look]
enabled = on
monitor_strip_colors = off

[color]
flag_command = lightgreen
flag_conditions = yellow
flag_post_action = lightblue
flag_regex = lightcyan
flag_return_code = lightmagenta
regex = white
replace = cyan
trigger = green
trigger_disabled = red

[trigger]
beep.arguments = ""
beep.command = "/exec -bg sh -c 'beep -f 750\;beep\;beep -f 750'"
beep.conditions = "${tg_highlight} || ${tg_msg_pv}"
beep.enabled = on
beep.hook = print
beep.post_action = none
beep.regex = ""
beep.return_code = ok
cmd_pass.arguments = "5000|input_text_display;5000|history_add;5000|irc_command_auth"
cmd_pass.command = ""
cmd_pass.conditions = ""
cmd_pass.enabled = on
cmd_pass.hook = modifier
cmd_pass.post_action = none
cmd_pass.regex = "==^((/(msg|quote) +nickserv +(id|identify|register|ghost +[^ ]+|release +[^ ]+|regain +[^ ]+) +)|/oper +[^ ]+ +|/quote +pass +|/set +[^ ]*password[^ ]* +|/secure +(passphrase|decrypt|set +[^ ]+) +)(.*)==$1$.*+"
cmd_pass.return_code = ok
lnotify-mattermost.arguments = "irc.mattermost.#senso2me;notify_message"
lnotify-mattermost.command = "/exec -norc -nosw notify-send -i weechat "${buffer.short_name}" "${tg_message}""
lnotify-mattermost.conditions = "${tg_highlight} == 0 && ${type} == channel && ${tg_tag_nick} != ${nick} && ${buffer.notify} > 0"
lnotify-mattermost.enabled = on
lnotify-mattermost.hook = print
lnotify-mattermost.post_action = none
lnotify-mattermost.regex = "/.*/${tg_message_nocolor}/ /&/&amp;/ /[\\]/&#92;/ /"/&quot;/ /</&lt;/ />/&gt;/"
lnotify-mattermost.return_code = ok
lnotify.arguments = ""
lnotify.command = "/exec -norc -nosw notify-send -i weechat "${buffer.short_name}" "${tg_message}""
lnotify.conditions = "(${tg_highlight} || ${tg_tag_notify} == private) && ${buffer.notify} > 0"
lnotify.enabled = on
lnotify.hook = print
lnotify.post_action = none
lnotify.regex = "/.*/${tg_message_nocolor}/ /&/&amp;/ /[\\]/&#92;/ /"/&quot;/ /</&lt;/ />/&gt;/"
lnotify.return_code = ok
msg_auth.arguments = "5000|irc_message_auth"
msg_auth.command = ""
msg_auth.conditions = ""
msg_auth.enabled = on
msg_auth.hook = modifier
msg_auth.post_action = none
msg_auth.regex = "==^(.*(id|identify|register|ghost +[^ ]+|release +[^ ]+) +)(.*)==$1$.*+"
msg_auth.return_code = ok
server_pass.arguments = "5000|input_text_display;5000|history_add"
server_pass.command = ""
server_pass.conditions = ""
server_pass.enabled = on
server_pass.hook = modifier
server_pass.post_action = none
server_pass.regex = "==^(/(server|connect) .*-(sasl_)?password=)([^ ]+)(.*)==$1$.*4$5"
server_pass.return_code = ok
