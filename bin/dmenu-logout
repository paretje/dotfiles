#!/bin/bash
choice=$(echo -e "logout\nshutdown\nreboot\nsuspend\nhibernate" | dmenu)
mod="Control_R"

case "$choice" in
	logout)
		xdotool key $mod+ctrl+shift+q &
		;;
	shutdown)
		nmcli device disconnect wlan0
		systemctl poweroff &
		;;
	reboot)
		nmcli device disconnect wlan0
		systemctl reboot &
		;;
	suspend)
		systemctl suspend &
		;;
	hibernate)
		systemctl hibernate &
		;;
esac
