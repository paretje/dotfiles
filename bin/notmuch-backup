#!/bin/sh -e
if [ "$HOST" = "parsley" -o "$HOST" = "chervil" ]; then
	folder="$HOME/vcs/senso2me/mail"
else
	folder="$HOME/vcs/personal/mail"
fi
notmuch dump --output="$folder/tags"
git -C "$folder" autocommit
