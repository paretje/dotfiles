#!/usr/bin/env bash
mru="$HOME/.cache/rofi-secret"
touch "$mru"
QUTE_URL=$(dmenu < "$mru")
QUTE_DATA_DIR="$HOME/.local/share/qutebrowser"
QUTE_FIFO="/dev/null"
. "$QUTE_DATA_DIR/userscripts/password_fill"
printf '%s' "$password" | xclip -i -selection clipboard
mv "$mru" "$mru.old"
echo "$QUTE_URL" > "$mru"
sed "/^$QUTE_URL\$/d" "$mru.old" >> "$mru"
rm "$mru.old"
